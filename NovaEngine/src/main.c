//#include "game.h"
#include <stdio.h>

int main() {
    // game_state_t game = {0};
    
    // if (!init_sdl(&game)) {
    //     printf("Failed to initialize SDL\n");
    //     cleanup(&game);
    //     return 1;
    // }
    // init_game(&game);
    
    // run_game(&game);
    // debug_info(&game);
    // cleanup(&game);
    for ( int i = 0; i < 10; i++ ) {
        printf("Hello, World!\n");
    }


    return 0;
}